<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Interviewer Records</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <style>
        body {
            font-family: Arial, sans-serif;
        }
    </style>
</head>
<body class="bg-gradient-to-b from-indigo-600 to-blue-300 min-h-screen">
    <header class="bg-blue-600 text-white py-4">
        <h1 class="text-2xl font-bold text-center">Interviewer Records</h1>
    </header>
    <nav class="bg-gray-200 py-4">
        <div class="container mx-auto flex justify-center">
            <a href="/" class="mx-2 text-blue-600 hover:underline">Home</a>
            <a href="/interviewer/create" class="mx-2 text-blue-600 hover:underline">Create</a>
            <a href="/interviewer" class="mx-2 text-blue-600 hover:underline">Read</a>
            <a href="/logout" class="mx-2 text-blue-600 hover:underline">Logout</a>
        </div>
    </nav>
    <div class="container mx-auto px-4 py-8">
        <table class="w-full bg-white shadow-lg rounded-lg">
            <thead>
                <tr class="bg-gray-200 text-gray-600 uppercase text-sm leading-normal">
                    <th class="py-3 px-6 text-left">Emp ID</th>
                    <th class="py-3 px-6 text-left">Interviewer Name</th>
                    <th class="py-3 px-6 text-left">Designation</th>
                    <th class="py-3 px-6 text-left">Contact</th>
                    <th class="py-3 px-6 text-left">Email</th>
                    <th class="py-3 px-6 text-left">Company ID</th>
                    <th class="py-3 px-6 text-center">Actions</th>
                </tr>
            </thead>
            <tbody>
                <% interviewerRecords.forEach(interviewer => { %>
                    <tr class="hover:bg-gray-100 border-b border-gray-200 py-4">
                        <td class="py-3 px-6 text-left whitespace-nowrap"><%= interviewer.emp_id %></td>
                        <td class="py-3 px-6 text-left"><%= interviewer.Interviewer_name %></td>
                        <td class="py-3 px-6 text-left"><%= interviewer.Designation %></td>
                        <td class="py-3 px-6 text-left"><%= interviewer.Contact %></td>
                        <td class="py-3 px-6 text-left"><%= interviewer.Email %></td>
                        <td class="py-3 px-6 text-left"><%= interviewer.c_id %></td>
                        <td class="py-3 px-6 text-center">
                            <a href="/interviewer/update/<%= interviewer.emp_id %>" class="text-indigo-600 hover:text-indigo-900 mr-2">Update</a>
                            <a href="/interviewer/delete/<%= interviewer.emp_id %>" class="text-red-600 hover:text-red-900 delete" onclick="return confirm('Are you sure you want to delete this interviewer?')">Delete</a>
                        </td>
                    </tr>
                <% }); %>
            </tbody>
        </table>
    </div>
    <footer class="bg-blue-600 text-white py-4 mt-8 fixed bottom-0 w-full">
        <div class="container mx-auto text-center">
            <p>&copy; 2024 SGBIT</p>
        </div>
    </footer>
</body>
</html>
